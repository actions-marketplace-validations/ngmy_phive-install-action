name: 'Install PHIVE tools'
author: 'Yuta Nagamiya'
description: 'Installs PHIVE tools and uploads them to the artifact store'
branding:
  icon: 'download'
  color: 'purple'
runs:
  using: 'composite'
  steps:
    - name: 'Install PHIVE'
      run: ${{ github.action_path }}/src/utils/retry.sh ${{ github.action_path }}/src/phive/install.sh
      shell: 'bash'
      # TODO: When "uses" becomes available, replace the above with the following
      #    - name: 'Install PHIVE'
      #      uses: 'nick-invision/retry@v2'
      #      with:
      #        timeout_minutes: 10
      #        max_attempts: 3
      #        command: |
      #          wget -O phive.phar https://phar.io/releases/phive.phar
      #          wget -O phive.phar.asc https://phar.io/releases/phive.phar.asc
      #          gpg --keyserver pool.sks-keyservers.net --recv-keys 0x9D8A98B29B2D5D79
      #          gpg --verify phive.phar.asc phive.phar
    - name: 'Install PHIVE tools'
      run: ${{ github.action_path }}/src/utils/retry.sh ${{ github.action_path }}/src/phive/install-tools.sh
      shell: 'bash'
      # TODO: When "uses" becomes available, replace the above with the following
      #    - name: 'Install PHIVE tools'
      #      uses: 'nick-invision/retry@v2'
      #      with:
      #        timeout_minutes: 10
      #        max_attempts: 3
      #        command: |
      #          yes | \
      #          php phive.phar --no-progress install \
      #            --copy
      # TODO: When "uses" is available, add the following
      #    - name: 'Upload PHIVE tools to artifact store'
      #      uses: 'actions/upload-artifact@master'
      #      with:
      #        name: 'tools'
      #        path: 'tools'
